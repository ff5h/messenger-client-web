import React, { FC } from 'react';
import { RegistrationPage } from '~/pages/RegistrationPage';

export const App: FC = () => {
  return (
    <div className='App'>
      <RegistrationPage />
    </div>
  );
};
